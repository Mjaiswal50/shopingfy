import { StrTruncatePipe } from './str-truncate.pipe';

describe('StrTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new StrTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
